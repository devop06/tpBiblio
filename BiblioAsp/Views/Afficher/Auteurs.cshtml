@model List<BiblioAsp.Models.Auteur> 
@{ 
    ViewBag.Title = "Auteurs";
}
<header>
       <h2>Liste des auteurs test</h2>
</header>
    <div>
        @if (Model.Count > 0)
        {
            <table>
                <tr>
                    <th>Nom de l'auteur</th>
                    <th>Livre</th>
                </tr>
                @foreach (BiblioAsp.Models.Auteur a in Model)
                {
                    <tr>
                        <td>@a.Nom</td>
                    </tr>
                }
            </table>
        }
        else
        {
            <p>Aucun auteur n'est présent dans notre base</p>
        }
    </div>